---
layout: layouts/base.vto
title: fabon's blog
eleventyImport:
  collections: ["posts"]
---

<main>
  <h1>posts</h1>
  <div class="posts">
    {{ for post of collections.posts.toReversed() }}
      <section>
        {{ include "time.vto" { date: post.date } }}
        <h2><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
        {{ include "tags.vto" { tags: post.data.tags } }}
      </section>
    {{ /for }}
  </div>
</main>
